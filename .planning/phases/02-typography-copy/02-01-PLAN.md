---
phase: 02-typography-copy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/[locale]/(landing)/landing-home.css
  - src/app/[locale]/(landing)/page.tsx
autonomous: false
requirements:
  - TYPE-01
  - TYPE-02

must_haves:
  truths:
    - "Hero heading renders at 5.5rem on 1440px desktop, creating 5.5:1 ratio against 1rem body text"
    - "Hero heading scales fluidly down to 2.75rem on mobile without media query overrides"
    - "Section h2 headings render at 2.8rem on desktop, visually subordinate to hero but dominant over body text"
    - "All headlines and descriptions communicate quiet authority -- short, declarative, no superlatives"
    - "Typography renders without overflow, awkward breaks, or layout issues at 1440px, 768px, and 375px"
  artifacts:
    - path: "src/app/[locale]/(landing)/landing-home.css"
      provides: "Updated clamp() values for h1 and h2, removed 768px font-size overrides"
      contains: "clamp(2.75rem"
    - path: "src/app/[locale]/(landing)/page.tsx"
      provides: "Rewritten copy with shorter, more confident headlines and descriptions"
      contains: "Admissions strategy"
  key_links:
    - from: "landing-home.css h1 rule"
      to: "page.tsx hero h1 content"
      via: "CSS selector .h-hero h1 styles the hero heading JSX"
      pattern: "clamp.*5\\.5rem"
    - from: "landing-home.css h2 rule"
      to: "page.tsx section h2 elements"
      via: "CSS selector .h-sect-head h2 styles all section headings"
      pattern: "clamp.*2\\.8rem"
---

<objective>
Update typography scale and rewrite all copy to establish quiet authority through visual hierarchy and editorial voice.

Purpose: The page communicates "most competent people in the room" through typography drama (hero dominates at 5.5:1 ratio) and copy restraint (shorter, more confident, declarative voice). This is the foundation for all subsequent visual work -- the words and their sizes set the emotional tone.

Output: Updated `landing-home.css` with new clamp() type scale, updated `page.tsx` with rewritten copy for all sections.
</objective>

<execution_context>
@/Users/iliazharnikov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/iliazharnikov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-typography-copy/02-RESEARCH.md
@src/app/[locale]/(landing)/landing-home.css
@src/app/[locale]/(landing)/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CSS typography scale with fluid clamp() values</name>
  <files>src/app/[locale]/(landing)/landing-home.css</files>
  <action>
Update the type scale in `landing-home.css` to create dramatic display-to-body hierarchy:

**1. Hero h1 (line ~200):**
Change `font-size: clamp(2.6rem, 5.8vw, 4.2rem)` to `font-size: clamp(2.75rem, 5vw + 1rem, 5.5rem)`
Change `line-height: 1.08` to `line-height: 1.05`
Change `letter-spacing: -0.03em` to `letter-spacing: -0.035em`
Change `margin: 0 0 32px` to `margin: 0 0 36px`

**2. Section h2 (lines ~391-401, the `.h-sect-head h2, .h-final h2` rule):**
Change `font-size: clamp(1.65rem, 3.8vw, 2.6rem)` to `font-size: clamp(1.5rem, 2.5vw + 0.75rem, 2.8rem)`
Change `line-height: 1.15` to `line-height: 1.12`
Change `letter-spacing: -0.02em` to `letter-spacing: -0.025em`

**3. Callout h2 (lines ~746-755, the `.h-callout-inner h2` rule):**
Update to match the new section h2 values:
Change `font-size: clamp(1.65rem, 3.8vw, 2.6rem)` to `font-size: clamp(1.5rem, 2.5vw + 0.75rem, 2.8rem)`
Change `line-height: 1.15` to `line-height: 1.12`
Change `letter-spacing: -0.02em` to `letter-spacing: -0.025em`

**4. Remove 768px font-size overrides (lines ~994-1015 area):**
Inside the `@media (max-width: 768px)` block, REMOVE these two rules entirely:
- `.landing-scope .shell .h-hero h1 { font-size: clamp(2.1rem, 8vw, 2.8rem); }` (line ~994-996)
- `.landing-scope .shell .h-sect-head h2, .landing-scope .shell .h-final h2 { font-size: clamp(1.5rem, 6vw, 2rem); }` (line ~1012-1015)

Keep ALL other 768px rules (padding, grid changes, etc.) intact. Only remove the font-size overrides.

**Do NOT touch:**
- Body text sizes (0.88-1.02rem range) -- the ratio comes from scaling UP display type
- `landing-atlas.css` -- home page typography lives entirely in `landing-home.css`
- `landing-animations.css` -- animation initial states are unchanged
- Card h3 sizes, kicker sizes, metric sizes -- these are already well-proportioned
  </action>
  <verify>
    <automated>cd /Users/iliazharnikov/Documents/GitHub/admission-atlas && grep -n "clamp(2.75rem" src/app/\[locale\]/\(landing\)/landing-home.css && grep -n "clamp(1.5rem, 2.5vw" src/app/\[locale\]/\(landing\)/landing-home.css && grep -c "clamp(2.1rem, 8vw" src/app/\[locale\]/\(landing\)/landing-home.css | grep "^0$" && grep -c "clamp(1.5rem, 6vw" src/app/\[locale\]/\(landing\)/landing-home.css | grep "^0$" && echo "PASS: Typography values updated, old overrides removed"</automated>
  </verify>
  <done>
Hero h1 uses clamp(2.75rem, 5vw + 1rem, 5.5rem) hitting 5.5rem on desktop. Section h2 uses clamp(1.5rem, 2.5vw + 0.75rem, 2.8rem). The 768px font-size overrides for h1 and h2 are removed (the new base clamp handles mobile gracefully). All other responsive rules remain intact.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite all copy for quiet authority voice</name>
  <files>src/app/[locale]/(landing)/page.tsx</files>
  <action>
Rewrite all headlines and descriptions in `page.tsx` to match the "most competent people in the room" voice: declarative, short (headlines under 8 words), specific, no superlatives, no exclamation marks.

**Voice rules:**
- Declarative, not promotional ("We architect school lists" not "We'll help you find schools!")
- Short assertions over long explanations
- Specificity signals competence ("school list architecture" not "school selection help")
- Em-dash for confident asides -- used sparingly
- No exclamation marks, no superlatives
- Implied expertise -- the copy assumes competence rather than arguing for it

**Section-by-section rewrites:**

**Hero badge:** Change "Strategy for ambitious families" to "Strategic admissions counsel"

**Hero h1:** Change from:
```
Your university application
<br />
deserves a <em>real</em>&nbsp;strategy
```
To:
```
Admissions strategy{' '}
built to <em>hold&nbsp;up</em>
```
Remove the `<br />`. Let natural wrapping + the 820px max-width container handle line breaks. The new clamp() sizing at 5.5rem will naturally wrap "Admissions strategy built to hold up" into two lines on desktop.

**Hero description:** Change from:
"We build defensible admissions plans — targeting the right schools, in the right regions, at the right price — so every application is positioned to win."
To:
"We architect school lists, position narratives, and manage execution across regions and budgets — so nothing is left to chance."

**Hero CTA buttons:** Change "Start Your Strategy Session" to "Start Your Strategy". Change "See How It Works" to "See the Process".

**Metrics section:** Refine labels for tighter voice:
- "4-Phase" / "Framework" -> "4-Phase" / "System" (system sounds more rigorous than framework)
- "1 : 1" / "Advisor Feedback" -> "1 : 1" / "Advisor Ratio" (ratio is tighter)
- "30+" / "Countries" -> keep as-is (already tight)
- "100%" / "Budget-Aware" -> "100%" / "Cost-Integrated" (more specific)

**Services section:**
- Kicker: "What We Do" -> keep as-is (already tight)
- h2: Change "End-to-end admissions strategy — not disconnected advice" to "Full-spectrum strategy — not disconnected advice". Remove the `<br />` and `&nbsp;`.
- Sub text: Change "Every engagement follows a structured system so nothing gets missed and every dollar of tuition exposure is intentional." to "Every engagement follows a structured system. Nothing gets missed. Every dollar of tuition exposure is intentional."

**Bento cards:** Tighten descriptions:
- Card 01 (School List Architecture): Keep title. Change description to "Balanced across regions, risk tiers, and what you can actually afford — not a top-heavy wish list."
- Card 02 (Major-Fit Positioning): Keep title. Change description to "We translate coursework, projects, and activities into the signals departments actually evaluate."
- Card 03 (Narrative Strategy): Keep title. Change description to "Essays that demonstrate direction and depth — not platitudes."
- Card 04 (Execution Management): Keep title. Change description to "Deadlines, drafts, and revision cycles on a single visible timeline."
- Card 05 (Budget & Aid): Keep title. Change description to "Tuition, aid probability, and scholarship odds — compared upfront, not after you fall in love with a school."
- Card 06 (Family Decisions): Keep title. Change description to "Trade-offs made explicit so decisions stay grounded in data."

**Process section:**
- Kicker: "How It Works" -> keep as-is
- h2: Change "From diagnostic to final submission" to "Diagnostic to submission". Remove `&nbsp;`.
- Timeline step descriptions -- tighten each:
  - Step 01 "Diagnostic Review": Change description to "Profile, goals, constraints, and strategic baseline — mapped in one session."
  - Step 02 "Portfolio Design": Change description to "School list and application strategy built across regions, tiers, and budgets."
  - Step 03 "Narrative & Materials": Change description to "Personal narrative, essays, and supplementals — drafted, reviewed, and refined."
  - Step 04 "Submission Control": Change description to "Pre-submit checks for coherence, fit signal, and quality under deadline."

**Outcomes section (dark):**
- Kicker: "The Difference Structure Makes" -> "What Structure Changes"
- h2: Change "What improves when the process is rigorous" to "When the process is rigorous". Remove `&nbsp;`.
- Outcome cards -- tighten:
  - "Sharper School Lists": Change description to "From top-heavy guesswork to balanced portfolios with realistic alternatives across regions."
  - "Stronger Major-Fit Signal": Change description to "Tighter alignment between your profile and what target programs actually evaluate."
  - "Cleaner Execution": Change description to "Milestone-based workflows eliminate rushed drafts and avoidable errors."
  - "Higher Decision Confidence": Change description to "Families choose with clearer trade-offs across selectivity, cost, and outcomes."

**Pricing section:**
- Kicker: "Programs" -> keep as-is
- h2: Change "Pick the level that matches your timeline" to "Matched to your timeline". Remove `&nbsp;`.
- Plan names and features: keep as-is (these are product names, not marketing copy)

**AI Engine Callout:**
- Kicker: "Free Tool" -> keep as-is
- h2: Change "Try the Neural Match Engine" to "Neural Match Engine". Remove `&nbsp;`.
- Description: Change to "Input your profile, target school, and budget. Our AI scores your fit and suggests alternatives worldwide — instant, no sign-up."
- CTA: "Launch the Engine" -> keep as-is

**FAQ section:**
- Kicker: "FAQ" -> keep as-is
- h2: Change "Common questions, straight answers" to "Common questions, direct answers". Remove `&nbsp;`.
- FAQ content: keep as-is (already well-written, Q&A format is inherently authoritative)

**Final CTA:**
- h2: Change "Ready to build a strategy that actually holds up?" to "Ready to build yours?". Remove the `<br />` and `&nbsp;`.
- Description: Change to "Share your profile, target majors, and budget range. We'll map the strongest path for your application cycle."
- CTA button: Change "Book Your Free Consultation" to "Book a Consultation"

**Footer tag:** Change "Strategic admissions guidance for high-stakes decisions." to "Strategic admissions counsel."

**IMPORTANT:** When removing `<br />` tags from headings, do NOT add any replacement whitespace handling unless needed. Let natural line wrapping work with the existing `max-width` constraints. The `max-width: 22ch` on h2 already controls line breaks well.

**IMPORTANT:** Preserve all `className`, `id`, and `aria-*` attributes exactly as they are. Only change text content and remove `<br />` tags where specified.
  </action>
  <verify>
    <automated>cd /Users/iliazharnikov/Documents/GitHub/admission-atlas && grep -c "Admissions strategy" src/app/\[locale\]/\(landing\)/page.tsx | grep -v "^0$" && grep -c "hold up" src/app/\[locale\]/\(landing\)/page.tsx | grep -v "^0$" && grep -c "positioned to win" src/app/\[locale\]/\(landing\)/page.tsx | grep "^0$" && echo "PASS: Copy updated with new voice"</automated>
  </verify>
  <done>
All headlines and descriptions are rewritten in the quietly authoritative voice. Hero heading is "Admissions strategy built to hold up". Unnecessary `<br />` tags are removed from headings. All copy is shorter, more declarative, and aligned with the "most competent people in the room" voice. All structural attributes (className, id, aria) are preserved.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of typography and copy at three breakpoints</name>
  <files>src/app/[locale]/(landing)/landing-home.css, src/app/[locale]/(landing)/page.tsx</files>
  <action>
Verify the visual result of typography and copy changes at three breakpoints. Updated typography scale (hero 5.5rem desktop, section h2 2.8rem) and rewritten copy across all sections. Two files modified: landing-home.css (type scale) and page.tsx (copy).

Steps for verification:
1. Start the dev server if not running: `npm run dev`
2. Open http://localhost:3000 in browser
3. NOTE: Sections are currently hidden by landing-animations.css (opacity: 0 from Phase 1). To see all content, either:
   - Enable "Prefers reduced motion" in browser DevTools (Settings > Rendering > Emulate CSS media feature prefers-reduced-motion: reduce)
   - OR add this in DevTools console: `document.querySelectorAll('.landing-scope .shell > div').forEach(el => { el.style.opacity = '1'; el.style.transform = 'none'; })`

**Desktop (1440px):**
- Hero h1 "Admissions strategy built to hold up" is visually dominant -- noticeably larger than before
- Hero h1 wraps naturally into ~2 lines without awkward orphans
- Section h2 headings are clearly subordinate to hero but larger than body text
- All copy reads as confident and concise -- no promotional language

**Tablet (768px):**
- Hero h1 scales down gracefully -- still large but no overflow
- No horizontal scrollbar appears
- Section headings wrap naturally without single-word orphan lines

**Mobile (375px):**
- Hero h1 at floor size (2.75rem / 44px) is still readable and dominant
- No text touches container edges
- Bento cards, outcome cards, and plan cards look balanced with new shorter copy
  </action>
  <verify>User approves the visual result at all three breakpoints.</verify>
  <done>Typography drama is visible (hero dominates), copy reads with quiet authority, no layout issues at any breakpoint. User has approved the result.</done>
</task>

</tasks>

<verification>
1. Hero h1 renders at clamp(2.75rem, 5vw + 1rem, 5.5rem) -- verified via DevTools computed style at 1440px viewport
2. Section h2 renders at clamp(1.5rem, 2.5vw + 0.75rem, 2.8rem) -- verified via DevTools
3. No 768px font-size overrides remain for h1 or h2 in landing-home.css
4. All copy uses declarative voice, headlines under ~8 words, no superlatives or exclamation marks
5. No horizontal overflow at any viewport width from 375px to 1440px
6. Dev server starts without errors: `npm run dev`
</verification>

<success_criteria>
- Hero heading at 5.5rem desktop creates dramatic 5.5:1 ratio against 1rem body text
- All headlines and descriptions rewritten for quiet authority voice
- Typography scales fluidly from mobile to desktop via single clamp() per level -- no media query font-size overrides
- No layout breakage at 1440px, 768px, or 375px breakpoints
- User approves visual result at checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/02-typography-copy/02-01-SUMMARY.md`
</output>
